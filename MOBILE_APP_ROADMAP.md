# JobPilot CN HTTPS 与手机App实施路线

更新时间：2026-08-21。

## 一、为什么浏览器显示“不安全”

当前访问地址是 `http://106.52.121.112`。HTTP内容和密码在传输途中没有TLS加密，所以浏览器必须标记“不安全”。这不表示服务器已经中毒，但在启用HTTPS前不应让真实用户输入密码、简历、身份证信息或付款信息。

项目已经使用Caddy并开放80、443端口。`deploy/Caddyfile`会按照生产环境中的`APP_DOMAIN`自动申请和续期证书，因此代码层不需要重新发明HTTPS。

### 正式解决顺序

1. 购买一个长期使用的主域名，并以备案主体本人完成域名实名认证。
2. 在腾讯云提交网站ICP备案。个人可以办理个人网站备案，不需要营业执照，但网站内容必须符合个人备案范围。
3. 建议在同一备案订单规划网站和APP信息；个人订单可提交1个网站和1个APP。
4. 备案通过后，在域名DNS中添加A记录，将域名指向`106.52.121.112`。
5. 服务器 `.env.production` 设置：

   ```env
   APP_DOMAIN=你的正式域名
   ACME_EMAIL=你的常用邮箱
   ```

6. 重新执行Docker Compose部署。Caddy会自动申请证书并把HTTP跳转到HTTPS。
7. 验收首页、游客登录、邮箱注册、简历上传与下载，并确认地址栏出现锁形图标。
8. 网站开通后按适用要求办理公安联网备案。

不要购买“仅IP访问”的临时证书作为主路线。正式网站、邮件发信域名、App API、深链和备案最终都需要稳定域名。

## 二、推荐的App技术路线

采用“一个后端 + 一套主要前端 + 三种发布形态”：

```text
腾讯云 API / 数据库 / 文件存储
             ↑ HTTPS
    ┌────────┼────────┐
    │        │        │
手机浏览器   Android   iOS
  / PWA    Capacitor Capacitor
```

选择Capacitor的原因是当前产品已经使用HTML、CSS和JavaScript，能够复用大部分页面和业务逻辑；需要相机、文件、通知等原生能力时再使用插件。它不是把线上网址简单塞进壳里，而是把前端资源打包进App，并通过HTTPS调用同一个后端API。

## 三、分阶段实现

### 阶段0：正式域名与HTTPS

完成ICP备案、DNS、Caddy自动证书、HTTPS全流程验证。没有这一阶段，不开始真实App账号联调。

### 阶段1：移动网页和PWA

- 手机真机检查登录、游客、简历上传、岗位列表、批量定制和面试页面。
- 增加Web App Manifest、192/512图标、启动图和主题色。
- 增加安全的Service Worker，只缓存静态壳，不缓存用户简历和私有API响应。
- 提供“添加到主屏幕”，先让用户以接近App的方式内测。

验收：主流安卓浏览器和iPhone Safari可用，页面无横向溢出，键盘弹起不遮挡输入框。

### 阶段2：App专用API和鉴权

- 将公开接口整理为版本化`/api/v1`。
- 网页继续使用HttpOnly Cookie；App使用短期Access Token + 可撤销Refresh Token。
- Token存入系统安全存储，不放普通LocalStorage。
- 增加设备列表、单设备退出、全部设备退出、刷新令牌轮换和异常登录日志。
- 只允许正式网站和App来源访问，配置严格CORS白名单。
- 文件上传、下载、语音、通知和深链全部经过权限校验。

### 阶段3：Android内测版

- 初始化Capacitor Android工程并配置应用ID，例如`cn.jobpilot.app`。
- 接入文件选择、麦克风、通知、返回键、深链和安全存储。
- 生成并妥善备份Android签名密钥；密钥和密码不得提交Git。
- 构建签名APK供少量测试者安装，再构建AAB用于应用市场。
- 在至少一台主流手机和不同Android版本上完成注册、上传、生成、下载、退出和升级测试。

### 阶段4：iOS内测版

- 需要Mac、Xcode和Apple开发者账号。
- 初始化Capacitor iOS工程，配置Bundle ID、签名、权限用途说明和通用链接。
- 使用真机验证文件、麦克风、通知、键盘和安全区。
- 上传App Store Connect，通过TestFlight邀请内测，稳定后提交审核。

### 阶段5：备案、上架与运营

- 办理APP备案，并确保备案主体、域名、应用名称、包名和实际功能一致。
- 准备隐私政策、用户协议、权限清单、第三方SDK清单、账号注销和个人信息删除。
- 接入崩溃监控、API错误监控、版本检测、灰度发布和最低版本控制。
- Android和iOS每次发布都增加版本号，保留可回滚的安装包与发布记录。

## 四、当前真实进度

| 能力 | 当前状态 |
|---|---|
| 手机响应式页面 | 有基础断点，未完成多机型真机验收 |
| PWA | 未开始 |
| App API | 业务REST接口可复用，移动鉴权未开始 |
| Android工程 | 未开始 |
| iOS工程 | 未开始 |
| APP备案与商店联调 | 未开始 |

最省时间的顺序是：域名与备案 → HTTPS → PWA内测 → App鉴权 → Android内测 → iOS TestFlight → 正式上架。不要同时启动Android和iOS商店审核，否则会在同一批基础问题上重复返工。
