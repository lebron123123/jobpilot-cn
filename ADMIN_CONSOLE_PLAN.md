# JobPilot CN 商用管理后台建设方案

更新时间：2026-08-21。本文只定义方案与实施顺序，尚未开始后台功能开发。

## 1. 结论

JobPilot CN 需要的是“平台运营后台”，不是把用户前台换个颜色再展示一遍。后台必须同时管理账号、业务数据、投递任务、AI成本、消息触达、套餐配额、风险审计和系统健康。

推荐方案：

- 复用 `ai-studyreport-local` 的独立后台入口、侧栏工作台、管理员二次验证、真实统计、审核发布和版本治理思路。
- 不原样复制其约26万字节的单文件 `admin.html`，改为页面、权限、API、审计和数据访问分层。
- 第一阶段先完成只读可观测性和低风险运营操作；涉及账号封禁、配额、重试、数据删除的操作必须审计并二次确认。
- 后台所有数据都来自真实业务记录，不做假数字卡片。
- 当前JSON存储只作为开发兼容层。正式商用数据层迁移到腾讯云MySQL/PostgreSQL时，后台页面不重写，只替换Repository实现。

## 2. 对本地 `ai-studyreport-local` 后台的拆解

参考文件：

- `C:\Users\HP\Desktop\ai-studyreport-local\admin.html`
- `C:\Users\HP\Desktop\ai-studyreport-local\functions\api\adminpass.js`
- `C:\Users\HP\Desktop\ai-studyreport-local\functions\api\stats.js`
- `C:\Users\HP\Desktop\ai-studyreport-local\functions\api\revlog.js`
- `C:\Users\HP\Desktop\ai-studyreport-local\functions\api\rag.js`
- `C:\Users\HP\Desktop\ai-studyreport-local\functions\api\paramgovernance.js`

### 2.1 可以直接迁移的产品思路

| 本地后台能力 | JobPilot迁移方式 |
|---|---|
| 独立 `admin.html` 与侧栏工作台 | 保留独立 `/admin` 入口，避免后台混进求职用户页面 |
| 普通登录后再做管理员二次验证 | 改成短时效“敏感操作授权”，高风险操作再次验证 |
| 运维统计必须读取真实计数 | 所有注册、活跃、生成、投递、AI和消息指标来自真实事件表 |
| 草稿 → 审核 → 发布 | 用于提示词、面试题模板、岗位词典、套餐和功能开关 |
| 配置版本、差异对比、恢复 | 每次后台配置变更保存前后值、操作者、原因和版本 |
| 用户权限与密级 | 改为平台角色 + `资源.动作` 权限，不只判断“是不是管理员” |
| 用户投稿/数据审核队列 | 改为反馈、投诉、异常投递、敏感数据请求和内容审核队列 |
| Agent调用记录和真实耗时 | 改为AI运行、模型、Token、费用、延迟和失败率看板 |
| 修改意见记录 | 改为客服工单、用户反馈和运营处理闭环 |

### 2.2 不应原样复制的部分

1. `admin.html` 页面、CSS和业务脚本全部堆在一个大文件中，后续多人开发和测试成本过高。
2. 多个API文件重复实现 `isAdmin()`，容易出现某个接口漏拦截或权限口径不一致。
3. 管理员名单依赖环境变量字符串，只能解决单一超级管理员，无法支持客服、运营、财务和审计角色。
4. 浏览器长期保存Bearer Token，并在每次请求携带后台密码，不适合作为正式商用后台安全模型。
5. 部分配置展示口径与真正限额分开维护，可能产生后台显示与实际执行不一致。
6. RAG、可研测算和PPT审核属于原项目业务，不迁移到求职产品。

### 2.3 迁移原则

- 可以直接复用：布局视觉、侧栏交互、空态/失败态、确认弹窗、真实统计卡片和审核流设计。
- 需要重写：管理员鉴权、权限中间件、审计、API、数据模型和JobPilot业务页面。
- 不复制：原项目RAG、测算、PPT、知识库等业务代码。

## 3. GitHub成熟项目对标

### 3.1 Twenty：对象、工作区与可扩展视图

项目：[twentyhq/twenty](https://github.com/twentyhq/twenty)

可借鉴：

- 将用户、岗位、投递、工单等都作为清晰业务对象，而不是一个巨型JSON。
- 列表支持筛选、排序、保存视图和详情侧栏。
- 工作区、对象、视图和工作流各自分层，便于未来扩展企业客户。

采用范围：借鉴对象模型和页面信息架构。仓库主体为AGPL且包含商业许可文件，不直接复制主体代码。

### 3.2 Cal.diy：资源级权限

项目：[calcom/cal.diy](https://github.com/calcom/cal.diy)；权限说明：[PERMISSIONS.md](https://github.com/calcom/cal.diy/blob/main/PERMISSIONS.md)

可借鉴：

- 权限采用 `resource.action`，例如 `user.read`、`user.disable`，比简单的管理员布尔值更适合商用。
- OWNER、ADMIN、MEMBER只是角色，真正执行时仍检查资源动作。
- 读、创建、修改、删除和成员管理分别授权。

采用范围：权限命名模式和MIT许可公共实现可参考；不依赖已移除的企业闭源功能。

### 3.3 Lago：用量、套餐、权益、订阅和账单分层

项目：[getlago/lago](https://github.com/getlago/lago)

可借鉴：

- `Usage Event → Meter → Plan → Entitlement → Subscription → Invoice` 分层。
- AI调用、简历解析、文档生成和投递准备都作为可去重的用量事件。
- 套餐权益与支付渠道解耦；即使暂时没有微信支付，也可以先正确记录配额和用量。
- 重试不能重复扣次数，所有用量事件需要幂等键。

采用范围：只借鉴计量和计费模型。Lago为AGPL-3.0，不直接复制代码。

### 3.4 PostHog：产品分析、功能开关和AI可观测性

项目：[PostHog/posthog](https://github.com/PostHog/posthog)

可借鉴：

- 注册→上传简历→分析→匹配→生成→入队→面试形成转化漏斗。
- 功能开关支持按用户、套餐或灰度比例发布新能力，并提供紧急关闭开关。
- 错误、日志、用户反馈和会话上下文联动排查。
- AI调用记录模型、Token、费用、延迟、结果状态和错误类型。

采用范围：先实现事件模型、漏斗、功能开关和AI追踪，不在第一版自建完整Session Replay。非 `ee/` 的MIT代码可在核对文件许可证后参考，企业目录不复制。

### 3.5 Infisical：最小权限、审计和敏感配置

项目：[Infisical/infisical](https://github.com/Infisical/infisical)

可借鉴：

- RBAC、临时授权、审批流和完整审计日志。
- 敏感配置只显示“已配置/未配置/最后更新时间”，不把密钥明文发到前端。
- 关键配置版本化、轮换、到期提醒和恢复。
- 高权限访问和数据查看都记录操作者、对象、来源IP和结果。

采用范围：借鉴安全边界；非 `ee/` 的MIT部分可核对后参考，企业目录不复制。

## 4. JobPilot后台导航与功能清单

### 4.1 总览驾驶舱

- 今日/7日/30日新增注册、活跃用户和游客。
- 简历上传、岗位导入、匹配、定制生成、投递入队和面试次数。
- 转化漏斗及各步骤失败率。
- 今日AI调用、Token、估算费用、平均延迟和失败率。
- 邮件/短信成功率、验证码限流和供应商错误。
- 浏览器执行任务运行中、失败、等待人工确认数量。
- 服务健康、版本号、最近部署、磁盘和备份状态。

### 4.2 用户与账号

- 按邮箱、手机号、用户ID、状态、套餐、注册时间搜索筛选。
- 查看基础资料、认证状态、最近登录、设备会话和用量摘要。
- 禁用/恢复账号、强制全部退出、重置限流、调整套餐和配额。
- 游客只显示数量和异常，不作为正式用户长期保留。
- 管理员默认看不到密码、验证码和完整简历正文。
- 查看完整敏感资料需要 `resume.read_sensitive` 权限、二次验证和访问原因。

### 4.3 简历与用户数据治理

- 文件类型、解析状态、大小、创建时间和失败原因。
- 解析失败重试、隔离恶意文件、生成文件过期清理。
- 用户数据导出、删除和注销申请队列。
- 只展示脱敏摘要；敏感正文访问单独审计。
- 数据保留期限和清理任务状态。

### 4.4 岗位、匹配与投递

- 岗位来源、公司、城市、薪资、创建人、重复状态和匹配分布。
- 匹配规则版本、分数异常和用户反馈。
- 定制简历生成任务、耗时、失败原因和下载状态。
- 投递队列按平台、状态、失败原因和人工确认状态筛选。
- 重复投递拦截记录和平台风控异常。
- 后台不得绕过用户批准执行最终提交。

### 4.5 浏览器自动化任务

- 任务ID、用户、站点、岗位、适配器版本、当前阶段和最后心跳。
- 查看脱敏日志、截图引用、字段识别结果和失败原因。
- 允许取消、重新准备、标记需人工处理；不提供“后台替用户确认投递”。
- 站点适配器成功率、最近改版时间和暂停开关。

### 4.6 AI、模型与内容配置

- 模型供应商、模型名称、场景、调用量、Token、费用、延迟和失败率。
- 提示词版本：草稿、测试、审核、灰度、发布、回滚。
- 岗位关键词、匹配规则、简历模板和面试题模板版本管理。
- 评测集和发布门禁，模型/提示词未通过基准评测不能全量发布。
- 后台只保存密钥引用和配置状态，不显示供应商密钥明文。

### 4.7 邮件与短信

- 腾讯云SES/SMS配置状态、模板ID、签名、地区和最后检查时间。
- 发送日志：渠道、用途、脱敏接收方、状态、错误码、耗时、重试次数。
- 验证码本身永不展示和记录明文。
- 模板审核状态、余量提醒、失败率告警和单用户限流。

### 4.8 套餐、权益与用量

- 免费版、体验版、专业版等套餐草稿与发布。
- 每月简历解析、定制生成、AI面试、岗位数量和投递准备次数。
- 用户订阅状态、开始/到期时间、赠送额度和人工调整原因。
- 用量事件必须带幂等键，重试不重复扣减。
- 第一阶段只做套餐和权益，不接真实支付；支付独立进入后续阶段。

### 4.9 运营分析与功能开关

- 注册、激活、留存、漏斗、功能使用率和失败率。
- 按渠道、设备、城市、套餐和版本筛选。
- 功能开关支持关闭、白名单、按用户/套餐开放和百分比灰度。
- 关键功能必须有一键停止开关：AI生成、邮件、短信、浏览器任务。

### 4.10 客服、反馈与内容审核

- 用户反馈、投诉、错误上报和功能建议。
- 状态：待处理、处理中、待用户、已解决、已关闭。
- 负责人、优先级、内部备注、用户可见回复和处理时长。
- 面试题、岗位模板、公共示例内容的草稿审核发布。

### 4.11 风险、安全与审计

- 登录失败、验证码滥用、异常游客创建、频繁下载和异常自动化任务。
- 管理员登录、二次验证、敏感查看、配置修改、账号操作和数据导出日志。
- 审计日志只追加，不允许后台直接修改或删除。
- 支持按操作者、动作、对象、结果、IP、时间范围查询导出。
- 数据导出、删除、角色变更和密钥轮换需要高风险二次确认。

### 4.12 系统运维

- 应用版本、Git提交、容器健康、启动时间和环境检查。
- 数据库、文件存储、邮件、短信和AI供应商连通性。
- 自动备份、最近成功备份、恢复演练和磁盘容量。
- 错误日志、慢请求、队列积压和告警记录。
- 生产密钥不通过后台明文编辑；后台只显示状态并引导到安全配置流程。

## 5. 权限模型

### 5.1 初始角色

| 角色 | 说明 |
|---|---|
| `super_admin` | 平台所有者；角色、系统和高风险操作 |
| `operations_admin` | 用户、业务、任务和活动运营，不看完整敏感简历 |
| `customer_support` | 工单、用户基础状态、强制退出；不能改套餐规则和系统配置 |
| `content_admin` | 提示词、面试题、岗位词典和内容发布 |
| `finance_viewer` | 套餐、订阅、用量和账务只读 |
| `security_auditor` | 安全事件和审计只读，不执行运营修改 |

### 5.2 权限命名

采用 `资源.动作`：

```text
dashboard.read
user.read
user.update
user.disable
user.force_logout
resume.read_metadata
resume.read_sensitive
application.read
application.update
automation.read
automation.retry
automation.cancel
delivery.read
delivery.retry
plan.read
plan.update
feature_flag.read
feature_flag.update
audit.read
audit.export
system.read
role.manage
```

前端隐藏按钮只改善体验，所有权限必须在服务器端再次校验。

## 6. 安全设计

1. 管理后台继续使用HttpOnly、Secure、SameSite Cookie，不在LocalStorage保存管理员Token。
2. 管理员必须是正式注册用户，游客永远不能进入后台。
3. 角色由数据库分配，不把管理员邮箱硬编码在前端。
4. 登录后台后，高风险操作需要短时效二次验证授权，默认15分钟失效。
5. 所有写操作要求同源校验/CSRF保护、权限校验和审计记录。
6. 列表默认脱敏邮箱、手机号、文件名和简历摘要。
7. 审计记录包括 `request_id`、管理员、权限、动作、对象、前后摘要、原因、IP、User-Agent、结果和时间。
8. 删除采用“申请→审核→执行→结果”流程；不在列表页放无保护的一键永久删除。
9. 后台不展示密码哈希、验证码、Session Token、腾讯云密钥和支付密钥。
10. 后台页面发送 `Cache-Control: no-store`，并禁止被第三方页面嵌入。

## 7. 推荐工程结构

```text
admin/
  index.html                 后台壳、侧栏、内容挂载点
  admin.css                  后台独立样式
  admin.js                   启动、路由、统一请求和错误边界
  pages/
    dashboard.js
    users.js
    resumes.js
    applications.js
    automation.js
    ai-operations.js
    deliveries.js
    plans.js
    analytics.js
    support.js
    audit.js
    system.js
server/
  admin-router.js            `/api/admin/*` 唯一入口
  admin-auth.js              requirePermission、二次授权
  audit-service.js           追加审计记录
  usage-service.js           统一用量事件和幂等
  repositories/
    store-repository.js      当前JSON兼容实现
    sql-repository.js        正式数据库实现
migrations/
  0001_admin_foundation.sql
tests/
  admin-auth.mjs
  admin-api.mjs
  admin-audit.mjs
  admin-browser.mjs
```

不会把所有后台代码继续塞进现有 `server.js`，否则权限和业务逻辑会迅速失控。

## 8. 建议数据表

第一批基础表：

- `admin_roles`
- `admin_permissions`
- `admin_role_permissions`
- `admin_user_roles`
- `admin_reauth_grants`
- `audit_logs`
- `usage_events`
- `usage_daily`
- `delivery_logs`
- `automation_jobs`
- `support_tickets`
- `feature_flags`
- `feature_flag_overrides`
- `system_incidents`
- `data_requests`

第二批商业表：

- `plans`
- `plan_entitlements`
- `subscriptions`
- `entitlement_balances`
- `payments`
- `invoices`
- `ai_runs`
- `prompt_versions`
- `evaluation_cases`
- `evaluation_runs`

## 9. 分阶段开发顺序

### 阶段A：后台安全底座

- `/admin` 独立入口。
- `super_admin` 初始化方式。
- 集中权限中间件、二次验证、请求ID和审计。
- 后台空壳、侧栏、路由、加载态、空态和错误态。

验收门槛：普通用户和游客访问返回403；管理员可以进入；越权API全部失败并留审计。

### 阶段B：可观测的运营MVP

- 总览驾驶舱。
- 用户列表/详情/禁用/恢复/强退。
- 简历、岗位、生成、投递和浏览器任务只读列表。
- 邮件短信发送日志。
- 系统健康、版本和备份状态。

验收门槛：所有数字能从真实记录复算，不出现摆设数据。

### 阶段C：AI、套餐与运营

- AI调用和成本。
- 提示词/规则/模板草稿审核发布。
- 套餐、权益、用量和人工调整。
- 客服反馈、功能开关和灰度发布。

验收门槛：配置变更可比较、可回滚；用量重试不重复扣减。

### 阶段D：商用增强

- 正式支付、账单和退款工作流。
- 数据导出/删除/注销流程。
- 多管理员细分角色、审计导出和安全告警。
- 数据库备份恢复演练、性能压测和灾备。

## 10. 第一轮建议实际开发范围

用户确认方案后，第一轮只实施阶段A和阶段B的核心：

1. 后台入口与独立模块结构。
2. 超级管理员、权限校验和15分钟二次授权。
3. 只追加审计日志。
4. 真实总览指标。
5. 用户列表、详情、禁用、恢复和强制退出。
6. 简历/岗位/投递/自动化任务只读运营视图。
7. 邮件短信状态和服务器健康视图。

本轮不做支付，不允许后台查看完整简历，不允许后台替用户确认投递，也不直接修改生产密钥。

## 11. 预计对现有评分的影响

方案文档本身不增加完成分。阶段A+B真实完成并上线后，预计：

- 管理后台：0 → 45—55。
- 安全与隐私：44 → 55左右。
- 稳定性与监控：28 → 45左右。
- 测试体系：56 → 62左右。
- 客服和用户反馈：5 → 15左右。

只有功能代码、权限回归、浏览器验收和腾讯云真实部署全部完成后才更新评分。

## 12. 开工前需要用户确认的三个产品决定

1. 第一位超级管理员绑定当前哪个注册邮箱；不在代码或文档中写密码。
2. 第一轮是否允许客服角色查看脱敏后的简历摘要；默认不允许查看正文。
3. 当前先继续使用JSON兼容层完成后台页面，还是先购买并接入腾讯云MySQL再做后台。推荐先做Repository兼容层和后台只读能力，同时准备MySQL迁移，避免页面等待基础设施。

确认后按“阶段A → 阶段B”开发，不一次性把全部12个页面做成只有外观、没有真实数据的空后台。
